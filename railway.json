{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate && { node scripts/deploy-once.mjs || true; } && npm run bot"
  }
}
